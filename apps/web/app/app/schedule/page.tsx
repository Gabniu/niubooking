// Ownership: Next staff schedule route; the legacy page remains a compatibility surface.
import { SchedulePage } from "../../components/schedule-page.js";

export const metadata = { title: "Schedule" };

export default function ScheduleRoute() {
  return <SchedulePage />;
}
