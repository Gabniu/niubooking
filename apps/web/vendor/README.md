# Vendored browser encoder

`qrcode.js` is a browser bundle of `qrcode@1.5.4` (`lib/browser.js`), generated for the static module-serving web shell. The upstream package is MIT licensed; keep this note beside the artifact when upgrading or regenerating it.
