# Feature Surface Matrix

## Purpose

Prevent backend capabilities from becoming invisible unfinished work and prevent frontend controls from becoming decorative promises.

Update this matrix whenever an API, command, event, worker, permission, route, or user workflow is added or materially changed.

## Classifications

| Classification | Required evidence |
|---|---|
| `user-facing` | Discoverable authorized UI, real contract/persistence integration, explicit states, responsive/accessibility verification |
| `integration-only` | Versioned contract, machine authorization, idempotency, audit, failure/retry/replay evidence; no UI unless an operator workflow requires it |
| `operations/internal` | Restricted operational path, runbook/observability, audit and recovery evidence; no public UI by default |
| `intentionally deferred` | Owner, reason, target phase, requirement/test reference, and no misleading frontend control |

## Status values

- `planned` — accepted scope but implementation has not started;
- `backend-only gap` — backend exists and a required user surface is missing;
- `frontend-only gap` — frontend exists without a complete backend contract;
- `in progress` — vertical slice is under construction;
- `verified` — required evidence has passed;
- `not applicable` — reviewed and UI or backend counterpart is intentionally unnecessary.

## Combined public-confirmation seam

The public booking adapter provides post-commit `booking.confirmed` and consent-aware `contact.captured` callback seams. `apps/worker/src/public-booking-hooks.ts` maps confirmation into reminder outbox planning and can compose contact verification when an issuer is supplied. `apps/worker/src/runtime-config.ts` validates the worker database URL, HTTPS public URL, and optional channel-provider configuration. Provider delivery remains outside the booking transaction; deployment-level process startup and provider composition are still tracked as a Batch C integration task.

## Foundation inventory

| Capability | Classification | Backend | Frontend | Status | Target / evidence |
|---|---|---|---|---|---|
| Layered batch verification | `operations/internal` | Source-size, parity, migration, lint, type, unit, build, real PostgreSQL, browser, and audit commands; checksum migration runner; serialized database integration runner prevents cross-schema DDL deadlocks | CI evidence only; no product UI required | `in progress` | BATCH-000; TEST-OPS-001; `npm run verify:batch`; approved-server PostgreSQL lane passed on 2026-08-14 and serial rerun passed on 2026-08-15; temporary staging image/probe smoke passed on 2026-08-15 |
| OIDC login start and callback | `user-facing` | Path-aware discovery with HTTPS/S256 validation, Authorization Code + PKCE exchange, state/nonce/iat/JWKS verification, exact local subject mapping, and opaque session creation | Next branded `/auth/sign-in` page and same-origin `/auth/login`/`/auth/callback` relays; public `/` and staff `/app` link to the branded page; legacy HTML bridge remains compatibility-only | `in progress` | Register the exact NIU Auth client redirect and exercise a live provider callback; shared state storage remains required before horizontal scaling |
| Current local tenant context | `user-facing` | Fastify `/v1/tenant-context/:tenantId` plus `/v1/workspaces`; opaque session lookup, active membership list, and transaction-local tenant admission | Next `/app` lists only session-authorized workspaces, supports explicit selection, then loads connected context and selected pack through typed contracts; loading/unauthenticated/denied/error states remain explicit | `in progress` | Live provider mapping, organization/branch display metadata, and production env wiring remain |
| Local membership denial | `user-facing` | Active-membership lookup runs inside a tenant transaction; missing, suspended, revoked, or cross-tenant membership returns the existing generic denial | Generic access-denied/recovery path | `in progress` | `authenticated-context.test.ts`; OIDC identity provisioning and live UI recovery remain |
| Session revocation/logout | `user-facing` | `booking_sessions.revoked_at` persistence adapter plus `POST /auth/logout` hash-based revocation and cookie clearing | `apps/web/src/auth-page.ts` probes the opaque session, exposes sign-out, and reloads after logout | `in progress` | Live provider callback and account-state journey remain |
| OIDC discovery/JWKS consumption | `integration-only` | Planned server adapter | None | `planned` | Auth integration tests |
| Health/readiness endpoints | `operations/internal` | API `/health/live` and database-backed `/health/ready`; worker counters remain injectable | None by default | `in progress` | Staging API probe foundation; worker external probe and deployment wiring remain |
| Audit event persistence | `operations/internal` | Migration `022_audit_events.sql`, tenant RLS, append-only reservation lifecycle writer in the occurrence transaction | Deferred admin audit viewer; no sensitive payloads exposed in staff response | `in progress` | `TEST-OPS-002`; viewer and replay tooling remain Batch 007 |
| Outbox delivery | `integration-only` | Planned worker and delivery ledger | None | `planned` | ADR-0009 contract tests |
| Voice Booking Provider API | `integration-only` | Planned versioned provider contract | Voice owns its caller workflow | `planned` | Phase 6; no Booking UI required |
| Staff application shell | `user-facing` | Tenant-context and authorized-workspace-list contract seams | Next App Router `/app` and `/app/schedule` with shared Hanken/Booking tokens, workspace picker, tenant-aware connected state, typed booking schedule, and selected-pack context; production web bridge keeps legacy HTML routes available while other staff routes migrate | `in progress` | Public/staff split, workspace selection, typed Next schedule, and production Next/legacy runtime bridge are implemented; live provider mapping and full route migration remain |
| Public guest booking | `user-facing` | Customer/service/availability/hold/confirm/manage lifecycle; migrations `014_booking_holds.sql`, `015_resources_allocations.sql`, `016_public_hold_resources.sql`, `017_booking_manage_capabilities.sql`, and `028_requirement_assignments.sql`; expiring hashed-token holds, signed/revocable manage capability, atomic customer/booking confirmation with selected resource allocations, typed requirement assignment persistence and confirmation-time revalidation, idempotent reschedule/cancel actions, optional pending contact capture, and public advisory resource availability (requirement-aware when a QR destination resolves a service, legacy capacity fallback when it has no requirements); `apps/api/src/runtime.ts` composes the pool, QR reader, and adapters from validated secrets | `apps/web/guest-booking.html` + `guest-booking-client.ts` + `guest-booking-page.ts` confirmation link; `guest-manage.html` + `guest-manage-page.ts` reschedule/cancel conversation | `in progress` | QR validity, opaque hold, conflict-safe transactional confirmation, resource allocation conflict mapping, assignment matching, signed capability validation, stale reminder cancellation, idempotent manage actions, and tenant policy enforcement verified; verification journey, provider delivery, and richer manage UX remain |
| Staff booking schedule | `user-facing` | Migrations `013_bookings.sql` and `015_resources_allocations.sql`, universal booking validation, tenant-scoped list/create/status persistence and API, scheduled-customer and resource exclusion constraints, advisory slot calculation, and stable resource assignment | Next `/app/schedule` + typed `bookings-client.ts` for admitted tenant context, loading/empty/denied/error states, and lifecycle status actions; legacy `apps/web/bookings.html` remains the compatibility create form with resource selection | `in progress` | Staff schedule, persisted resource inventory/allocations, availability endpoint, and conflict-safe commit foundation verified; resource administration UI, reschedule, reminder event wiring, and full route migration remain |
| Universal occurrences and reservations | `user-facing` | Domain occurrence/reservation invariants, migrations `019_occurrences_reservations.sql`, `020_public_reservation_idempotency.sql`, `021_communication_occurrence_subjects.sql`, and `022_audit_events.sql`, tenant RLS tables, atomic capacity admission/release, retry-safe public reservation adapter, canonical customer/contact capture, post-commit occurrence event callback, lifecycle transition rules, audit evidence, and tenant-authorized/public API routes | Next `/app/occurrences` + typed staff client/page with occurrence creation, capacity display, expandable reservation list, and status controls; legacy `apps/web/occurrences.html` remains a compatibility bridge; `apps/web/guest-occurrence.html` + public discovery/reservation client/page with missing, unavailable, loading, consent, and success states | `in progress` | BATCH-001; `TEST-SCH-001`, `TEST-SCH-002`, `TEST-SCH-005`, `TEST-SCH-006`, `TEST-SCH-008`; approved PostgreSQL migration and concurrency proof passed, Next staff surface and CI/release repetition remain |
| Resource inventory and availability | `user-facing` | `packages/domain/src/resources.ts`, `packages/database/src/resources.ts`, migrations `015_resources_allocations.sql`, `016_public_hold_resources.sql`, and `027_resource_capabilities.sql`, tenant-scoped resource CRUD/status, capability metadata, and advisory availability routes | Next `/app/resources` + typed `resources-client.ts` manage inventory, capabilities, search, and active/inactive lifecycle; legacy `apps/web/resources.html` remains a compatibility bridge; staff booking resource selector, public QR requirement-aware availability suggestions with assignment-aware holds, human requirement summary, and service-composition candidate-time panel consume the same contracts | `in progress` | Tenant isolation, active-resource filtering, stable capacity assignment, half-open overlap protection, capability matching, real admin controls, Next inventory surface, resource-aware public commit, typed assignment revalidation, and opaque-ID public summaries verified; resource-specific public presentation remains intentionally limited |
| Service definitions, variants, and requirement slots | `user-facing` | `packages/domain/src/services.ts`, `service-variants.ts`, tenant-scoped persistence, migrations `023_service_definitions.sql` and `024_service_variants_requirements.sql`, tenant-safe list/create/status routes, requirement-aware advisory availability adapter, and confirmation-time assignment validator | Next `/app/services` + typed `services-client.ts` catalog and `/app/service-composition` typed variants, requirement slots, status controls, and advisory candidates; legacy `apps/web/services.html` and `service-composition.html` remain compatibility bridges | `in progress` | Catalog, composition, capability matching, explainable advisory candidates, public requirement summaries, Next catalog surface, and public hold assignment revalidation verified; pricing policy and pack defaults remain next; TEST-CORE-003; TEST-SCH-010 |
| Industry Pack validation, catalog, organization selection, and materialization | `user-facing` | `packages/domain/src/industry-packs.ts`, deterministic materialization plan, versioned fixture registry, bounded override validation, tenant persistence/audit in migrations `025_industry_pack_settings.sql` and `026_pack_materialization_metadata.sql`, typed requirement slots, read-only `/v1/industry-packs` catalog with safe navigation/dashboard contributions, tenant GET/PUT selection, and authorized materialization command | Next `/app/packs` catalog and `/app/pack-settings` typed tenant selection, approved overrides, and materialize-services action; legacy `apps/web/packs.html` and `pack-settings.html` remain compatibility bridges; service composition candidate-time panel | `in progress` | Driving School, Dental, Hospital, Fitness, Transport, Charter, and Salon fixtures validate; Next catalog and pack-specific contributions are visible; materialized slots produce explainable candidates and assignment-aware holds revalidate at confirmation; richer pack workflows, branch-level overrides, upgrade migrations, authenticated shell admission, and richer public assignment remain; TEST-PACK-001; TEST-SCH-010; BATCH-003 |

| QR booking destinations | `user-facing` | `packages/domain/src/qr.ts`, `packages/database/src/qr-destinations.ts`, migration `002_qr_destinations.sql`, public resolver, validated authenticated list/create/status routes | Next `/app/qr-studio` + typed `qr-admin-client.ts` authorized selector, create, pause, activate, and revoke controls; `apps/web/qr-studio.html` remains a compatibility bridge | `in progress` | Persistence, validation, and staff lifecycle controls verified; rotate and full management UI next; ADR-0013; TEST-QR-001 |
| QR Print Studio | `user-facing` | Typed preview constraints + print-safe SVG generator; browser download/print actions | Next `/app/qr-studio` typed destination-aware preview, diagnostics, print, SVG download, and lifecycle controls; `apps/web/qr-studio.html` remains a compatibility bridge | `in progress` | SVG generation, quiet-zone/contrast diagnostics, browser print surface, and QR encoder complete; scan-device acceptance next; ADR-0013; TEST-QR-002 |
| Feedback campaigns | `user-facing` | Domain eligibility policy, migration `003_communications.sql`, versioned survey templates/responses, opt-out, attribution, delivery events, outbox jobs | Client feedback link, post-appointment and general campaign flows, staff response view next | `in progress` | Communication foundation + outbox; ADR-0014; TEST-COMMS-001 |
| Appointment and occurrence reminders | `user-facing` | Reminder policy, organization settings/rules, tenant-scoped rule replacement persistence, migrations `004_communication_outbox.sql`, `018_booking_change_policy.sql`, and `021_communication_occurrence_subjects.sql`, atomic claims, idempotency, completion states, post-commit booking/occurrence hook composition, and lifecycle cancellation for completed/cancelled/no-show reservations | `apps/web/communications.html` + `communications-page.ts` organization controls; public manage link, client reschedule/cancel screen, and QR occurrence reservation flow | `in progress` | Organization policy, occurrence subject metadata, lifecycle invalidation, post-commit reminder planning, and development-provider delivery smoke verified; production provider delivery and richer policy previews remain; Batch C; ADR-0014; TEST-COMMS-002; TEST-SCH-009 |
| Communication worker | `operations/internal` | `apps/worker` provider-neutral claim/suppress/send/complete orchestration with late-bound recipient resolution; contact destinations stay in worker memory and missing contacts are suppressed | None; operational metrics and replay tooling next | `in progress` | Outbox worker foundation; ADR-0014 |
| Booking communication event producers | `integration-only` | `apps/worker/src/booking-communication-events.ts` handles created/rescheduled/completed/cancelled events plus explicit `feedback.requested` general-client events into deterministic outbox mutations; `apps/worker/src/public-booking-hooks.ts` maps post-commit public confirmation facts into the created-event handler and optionally composes contact verification | None; replay dashboard and deployment-level composition remain | `in progress` | Public QR confirmation now reaches reminder planning through a tenant transaction; verification remains host-configured and provider-neutral; ADR-0014 |
| Communication provider adapters | `integration-only` | Provider-neutral channel router passes tenant/job identity, resolved recipient, and idempotency key to Email/SMS/Voice adapters; feedback delivery wrapper issues one opaque capability per idempotent job and adds the public URL without giving providers Booking write access | None; provider configuration and delivery observability next | `in progress` | Feedback capability issuance, provider composition, and late-bound recipient slice; ADR-0014 |
| Customer contact resolution | `integration-only` | Migrations `008_customer_contact_methods.sql` through `012_customer_contact_fk.sql` provide tenant-scoped, canonical-profile-linked, verified/consented, opt-out-aware late resolution; challenge codes are hashed, expiring, single-use, and attempt-limited; `apps/worker/src/contact-verification-delivery.ts` sends the code only as an ephemeral provider payload and revokes on delivery failure | `apps/web/communications.html` loads the real customer profile selector and lists masked methods with customer names; `contact-verification.html` provides the public code-entry surface | `in progress` | Canonical customer linking, challenge API, public verification, and provider-delivery adapter verified; live provider composition and deployment wiring remain; ADR-0014 |
| Customer profiles | `user-facing` | Migration `011_customers.sql` and `packages/database/src/customer-profiles.ts` provide tenant-scoped canonical customer identity with active/archived lifecycle; migration 012 enforces same-tenant contact ownership for new writes; detail reads and updates retain tenant predicates | Next `/app/customers` + typed `customers-client.ts` search, list, create, edit, archive, and restore controls; legacy `apps/web/customers.html` remains a compatibility bridge; communications uses profiles for contact capture | `in progress` | Profile foundation, lifecycle, detail/edit, Next management surface, and contact linking slice verified; live auth/bootstrap and industry attributes remain |
| Development communication provider | `operations/internal` | `apps/worker/src/development-provider.ts` emits normalized receipts without external delivery | None; local worker smoke-test harness next | `in progress` | Provider contract validation; ADR-0014 |
| HTTP channel provider | `integration-only` | HTTPS-only, API-key, idempotency-header, timeout, and retry classification adapter | None; deployment secrets and provider health checks next | `in progress` | Production-shaped adapter; ADR-0014 |
| Provider configuration and retry policy | `operations/internal` | `apps/worker/src/provider-config.ts` validates environment configuration; `retry-policy.ts` bounds transient retries | None; deployment health/readiness surface next | `in progress` | Batch A production readiness |
| Worker health and delivery observability | `operations/internal` | `apps/worker/src/worker-health.ts` + `worker-runtime.ts` expose redacted readiness, counters, one-tick orchestration, and injectable recipient resolution | Deployment probe/metrics endpoint remains next | `verified` | Batch A production readiness; 296 automated tests |
| Feedback templates and responses | `user-facing` | Versioned template policy, migrations `005_feedback_templates_responses.sql`, `006_feedback_presentation.sql`, and `007_feedback_delivery_capabilities.sql`; idempotent opaque capability issuance, expiry, one-response boundary, approved-template answer validation at the public API boundary, campaign/template admin, response reporting, aggregate analytics, and creator-selected presentation contract | `apps/web/feedback.html` + `feedback-page.ts` public compact, guided-step, and one-question conversational flows; admin authoring controls; feedback clients/form/reporting/analytics adapters | `in progress` | Question count is intentionally uncapped; creator selects compact scroll, questions-per-step, or conversational progression. Capability issuance and public answer validation are now wired; live auth/bootstrap and production provider wiring remain; ADR-0014 |
| Feedback campaign policy | `user-facing` | `packages/domain/src/feedback-policy.ts` validates audience, frequency caps, template version, and expiry | Organization campaign controls next | `in progress` | Batch B configuration safety |
| Feedback management workspace | `user-facing` | Campaign/template/reporting contracts and authorization routes; validated tenant-scoped campaign creation and reversible status changes; template validation at HTTP and persistence boundaries; matching-template gate before activation; presentation persistence | Next `/app/feedback` typed tenant page with campaign creation, enable/pause controls, uncapped authoring, creator-selected experience controls, aggregate analytics, and privacy-safe response summaries; `apps/web/feedback-admin.html` remains a compatibility bridge | `in progress` | Campaign policy and authoring slice verified; live auth/tenant bootstrap, capability issuance, and delivery wiring remain next; Batch B; ADR-0014 |
| Communication settings workspace | `user-facing` | Communication settings API and policy contracts; reminder-rule persistence now replaces the tenant rule set through the persistence port; booking-change enablement and minimum-notice policy persists with tenant settings | Next `/app/communications` typed tenant page with reminder rules, booking-change policy, feedback defaults, and masked contact-method management; `apps/web/communications.html` remains a compatibility bridge | `in progress` | Organization settings and client-change policy vertical slice; client reminder/reschedule journey and live auth/tenant bootstrap remain next |

## Pull-request audit

For each changed capability:

1. identify its authoritative backend contract and classification;
2. locate the required frontend route/component, or record why no UI is appropriate;
3. verify authorization and tenant behavior on both sides;
4. exercise loading, empty, validation, failure, degraded, pending, and success states that apply;
5. verify the real API/persistence boundary, not only mocks;
6. record desktop, tablet, mobile, keyboard, focus, zoom, and reduced-motion evidence for user-facing changes;
7. remove or track stale controls and backend-only gaps before declaring completion.
