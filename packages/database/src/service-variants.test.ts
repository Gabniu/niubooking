import assert from "node:assert/strict";
import test from "node:test";
import { createServiceRequirement, createServiceVariant, listServiceRequirements, listServiceVariants, setServiceRequirementStatus, setServiceVariantStatus } from "./service-variants.js";

const variantRow = { id: "variant-1", tenant_id: "tenant-1", service_id: "service-1", name: "Manual lesson", duration_minutes: null, buffer_before_minutes: null, buffer_after_minutes: null, price_cents: null, currency: null, status: "active" as const };
const requirementRow = { id: "requirement-1", tenant_id: "tenant-1", service_id: "service-1", variant_id: "variant-1", kind: "resource" as const, label: "Instructor", quantity: 1, resource_type: "staff", capability_key: "licence.manual", status: "active" as const };
test("lists and creates variants and requirements", async () => { const executor = { query: async <T>(sql: string) => (sql.includes("service_requirements") ? [requirementRow] : [variantRow]) as T[] }; assert.equal((await listServiceVariants(executor, "tenant-1", "service-1"))[0]?.name, "Manual lesson"); assert.equal((await createServiceVariant(executor, { id: "variant-1", tenantId: "tenant-1", serviceId: "service-1", name: "Manual lesson" })).status, "active"); assert.equal((await listServiceRequirements(executor, "tenant-1", "service-1", "variant-1"))[0]?.capabilityKey, "licence.manual"); assert.equal((await createServiceRequirement(executor, { id: "requirement-1", tenantId: "tenant-1", serviceId: "service-1", variantId: "variant-1", kind: "resource", label: "Instructor", capabilityKey: "licence.manual" })).status, "active"); });
test("changes composition status within a tenant", async () => { const executor = { query: async <T>() => [{ id: "composition-1" }] as T[] }; assert.equal(await setServiceVariantStatus(executor, "tenant-1", "variant-1", "inactive"), true); assert.equal(await setServiceRequirementStatus(executor, "tenant-1", "requirement-1", "inactive"), true); });
